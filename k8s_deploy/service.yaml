apiVersion: v1
kind: Service
metadata:
  name: account-mgr-service
  namespace: pay-ns
  labels:
    app: account-mgr
spec:
  type: NodePort
  selector:
    app: account-mgr
  ports:
  - name: rpc
    port: 8080
    targetPort: 8080
    nodePort: 30881
    protocol: TCP
